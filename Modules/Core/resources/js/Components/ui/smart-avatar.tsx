import * as React from 'react';

interface SmartAvatarProps {
    children: React.ReactNode;
}

export function SmartAvatar({ children }: SmartAvatarProps) {
    // Placeholder for future logic (e.g., status, tooltips)
    return <>{children}</>;
}
