import React from 'react';

const RentalItemsCard = (props: any) => (
  <div className="rental-items-card-placeholder">
    {props.children || 'Rental Items Card'}
  </div>
);

export default RentalItemsCard;





















