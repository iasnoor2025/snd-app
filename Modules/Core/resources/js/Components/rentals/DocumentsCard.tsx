import React from 'react';

const DocumentsCard = (props: any) => (
  <div className="documents-card-placeholder">
    {props.children || 'Documents Card'}
  </div>
);

export default DocumentsCard;





















